1. Install the git and create a repo on bitbucket 
2. Store bitbucket repo link
3. configure the git mean setup the git in local laptop

   3.1 first configure the user name this name is same as bitbucket name
       git config --global user.name "Naman Khandelwal"
   3.2 if u want to change the user name than use this command
       git congif --global --replace-all user.name "other name"
   
   3.3 And than config the user email 
       git config --global user.email "naman.khandelwal@intimetec.com

   3.4 And than config the code editor nano/notpad/emacs
       git config --global core.editor emacs
  
   3.5 And than check configuration  
       git config --list


4. Now initillised the reposetry in the  directory 
  
   4.1 first initillised project libebaby
       git init
   
   4.2 to set remort origin 
       git remote add origin (here paste the bitbucket repo link)
   
   4.3 for checking that the origin is confirm 
       git remote -v
   
   4.4 check git status helps track the untracked files in repo
       git status
   
   4.5 after creating every change than add change in repo
       git add .

   4.6 After add than commit that change commit mean you can right what change u done and by
       git commit -m "Here write commit "
   
   4.7 Than push in original branch (master branch)
       git push -u -f origin master

5 For changing branch and creating new branch 
  git checkout -b (here write new branch name) master
 

